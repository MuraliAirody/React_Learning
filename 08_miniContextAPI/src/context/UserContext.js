import React from "react";


const UserContext = React.createContext(); //context can be different for user,login,admin 

export default UserContext;  //each and every context are a provider (variable provider)

