import React from 'react';
import ReactDOM from 'react-dom/client'; //DOM created bu react
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root')); //created root over id = root in index.html
root.render(
    <App />  
);


